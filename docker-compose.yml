version: '3.3'
services:
  db:
    container_name: db
    image: postgres:alpine
    restart: always
    env_file: .env
    volumes:
      - './data:/var/lib/postgresql/data'
    ports:
      - '5432:5432'
